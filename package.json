{
  "name": "@pct-org/mongo-models",
  "version": "0.0.3",
  "private": false,
  "description": "Models for Mongo",
  "license": "MIT",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/pct-org/mongo-models.git"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json -c tslint.json"
  },
  "dependencies": {
    "graphql": "^14.5.4",
    "mongoose": "^5.7.0",
    "type-graphql": "^0.17.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "ts-node": "8.1.0",
    "tsc-watch": "2.2.1",
    "tsconfig-paths": "3.8.0",
    "tslint": "5.16.0",
    "typescript": "3.4.3"
  }
}
